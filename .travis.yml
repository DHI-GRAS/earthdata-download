language: python
python:
  - "2.7"
  - "3.6"
before_install:
  - pip install -U pip
install:
  - pip install pytest-cov
  - pip install -r requirements_full.txt
  - pip install -e .[test]
script: 
  - py.test --cov=./ -v
after_success: 
  - codecov
